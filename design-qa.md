# Design QA — 漢字学習アプリ

final result: passed

## 比較対象と証拠

- 正式な参照: `output/mockups/04-notebook-gothic-white.png`（1680×936）。4画面の生成モックアップ。
- 実装: SwiftUI / iPhone 17 Pro / iOS 26.5、402×874 pt、3倍密度の1206×2622 px。
- 最新画面: `output/verification/import.png`, `list.png`, `question.png`, `answer.png`, `complete.png`。
- 同一画像内の全体比較: `output/verification/comparison.png`。上段に参照、下段に実装4画面をアスペクト比を維持して390 px幅へ縮小して配置。
- 答え画面は原寸画像でもフォント、意味、選択コントロールを拡大確認。
- 参照にはOS領域がなく、生成された各パネルの高さも計画値と多少異なるため、縦方向のピクセル一致ではなくアプリ内容領域の構成を評価。
- 状態差: 参照は24件、検証CSVは5件。実装の答え画面は一覧から直接開いた状態のため、問題数を表示せず下端が「一覧に戻る」。問題画面は習熟度で1件に絞った状態。いずれも仕様どおり。

## 必須観点

- フォント: システムの日本語ゴシック体。問題144 pt、短い答え80 pt、見出しを太字にし、意味と操作ラベルは読みやすい通常サイズ。明朝体を使用していない。
- 配置: 20〜24 ptの左右余白、薄い行区切り、カテゴリーを問題の下に配置。答えの3段階選択と次へ操作を分離。サンプル5件は通常の一覧状態で収まり、長い一覧や意味はスクロールできる。
- 色: ほぼ白の背景、落ち着いた緑の主ボタン、薄い黄・青・緑の習熟度。文字とチェック・枠線でも選択を表現。
- 画像・アイコン: SF Symbolsの書類、情報、矢印、チェックを使用。モックアップ外側のタイトルや葉の装飾はアプリ画面外のプレゼンテーション要素なので実装対象外。
- 文言: CSVの4列、初期状態の説明、習熟度の3表記、問題・答え・意味・一覧へ戻るの対応を確認。取り込み後件数とセッション進捗は実データ由来。

## 修正履歴

1. UIテストで完了画面が表示されない問題を発見。StudySessionの等価判定がIDだけを比較しており、内部状態変更を正しく区別できなかった。全フィールドのHashable/Equatable合成に変更。完了・再開・複数問題の遷移のテストに合格。
2. 最初の視覚比較では問題・答えが参照より小さかったため120→144 pt、60→80 ptに変更。一覧の行高を調整し、取り込み通知を閉じられるようにした。
3. 最新キャプチャで文字の強調、5行の一覧、下端の操作領域、日本語の欠けがないことを再確認。未解決のP0/P1/P2はなし。

## 検証

- iOS Simulator向けDebugビルド成功。
- CSV解析: UTF-8/BOM、Shift JIS、列順変更、カンマ、引用符、セル内改行、空・不正データの拒否。
- データ: 初期習熟度、カテゴリー・習熟度フィルター、保存・再読み込み、破損ファイルの上書き防止、セッションの問題集合と状態遷移。
- UI自動テスト: 初回空状態、CSVプレビューからの取り込み確定、通知を閉じる、一覧→答え、習熟度変更、アプリ再起動での保持、フィルター、問題→答え→完了、複数問題で次へ進むと答えが隠れること。
- 最新テスト結果: `/private/tmp/KanjiBuildUp-verified.xcresult`。`output/verification/verified-ui-tests/manifest.json` に画面添付の記録。

## 検証範囲と残る調整

- OSのファイルピッカーから実際のiCloud等のファイルを選択する部分は未自動検証。UIテストはDEBUG限定のCSV fixtureで取り込みプレビューを用意し、確定後は本来の保存処理を使用する。
- 実機、横向き、iPad、アクセシビリティの最大文字サイズは未検証。
- P3: 生成モックアップとはOSセーフエリアやフォント描画に起因する細かな余白差がある。機能と読める配置を優先し、固定画像による再現はしていない。

## 手入力追加の検証

- ユーザーの追加要望により、一覧の「追加」から手入力とCSV取り込みを選択可能にした。デザインの忠実な再現より機能を優先する方針に変更。
- カテゴリー・問題・答えを必須、意味を任意とするフォーム。カテゴリー候補と自由入力、初期習熟度「がんばるぞ」、入力中の破棄確認を実装。
- UIテストで空欄・空白のみの保存禁止、日本語入力、カテゴリー候補選択、保存、答え表示、再起動後の保持、キャンセル時に追加されないことを確認。iOS Simulatorビルドとテスト成功。
- 手入力の結果: `/private/tmp/KanjiBuildUp-manual-clean.xcresult`。画面: `output/verification/manual-entry/9D71F5DA-CEE1-4273-88D0-BA38889E396A.png`。日本語の欠けや操作の重なりがないことを目視確認。
- CSVから学習までの既存UIテストも、新しい追加メニュー経由で成功（`/private/tmp/KanjiBuildUp-manual.xcresult`）。上記の旧一覧キャプチャは追加メニュー導入前のもの。
